export type Coordinates  = {
    ville : string,
    latitude : number,
    longitude : number,
    checkWeather : boolean
}

export type City = {
    id: number,
    name: string,
    latitude: number,
    longitude: number,
    country_code: string,
    admin1 : string
}